# === 기존 의존성 (규칙 기반 엔진) ===
fastapi==0.115.6
uvicorn==0.34.0
pydantic==2.10.4
httpx>=0.27.0
python-dotenv>=1.0.0

# === LangGraph 엔진 의존성 (USE_LANGGRAPH=true 시 필요) ===
# 본 프로젝트의 기본 LLM provider는 자체 RunPod/Ollama 서버이므로
# 상용 API SDK(google-genai 등)는 기본 의존성에서 제외합니다.
# (비권장) AI_PROVIDER=gemini 사용 시에만 별도 설치:
#     pip install google-genai>=1.0.0
langgraph>=0.2.0
langchain-community>=0.2.0
langchain-huggingface>=0.1.0
langchain-text-splitters>=0.2.0
chromadb==0.5.23

# === Phase 10.51+ — PDF/DOCX 첨부 본문 추출 ===
# 텍스트 레이어 PDF / Word(.docx) 문서에서 본문 텍스트 추출.
# 이미지 PDF/OCR 은 미지원 (텍스트 레이어 없으면 "본문 추출 실패" 안내).
pypdf>=4.0.0
python-docx>=1.0.0
