# 로컬 개발용: PostgreSQL 없이 H2 인메모리 DB (spring.profiles.active=local)
spring:
  datasource:
    url: jdbc:h2:mem:legalreview;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
